// スペース
const spacing = [2, 4, 8, 16, 24, 32, 64, 128, 256, 512];

// 色
const colors = {
  primary: "#72D2FF",
  white: "#FFFFFF",
  black: "#000000",
  background: {
    light: "#FFFFFF",
    main: "#E4E4E4",
  },
  lightGray: "#e0e0e0",
};

// 文字サイズ
const fontSizes = {
  small: 12,
  medium: 16,
  large: 20,
};

const theme = {
  colors,
  fontSizes,
  spacing,
};

export default theme;
