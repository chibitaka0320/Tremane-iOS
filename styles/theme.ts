// スペース
const spacing = [2, 4, 8, 16, 32, 64, 128, 256, 512];

// 色
const colors = {
  primary: "#72D2FF",
  background: {
    light: "#FFFFFF",
    main: "#E4E4E4",
  },
};

// 文字サイズ
const fontSizes = {
  small: 12,
  midium: 16,
  large: 20,
};

const theme = {
  colors,
  fontSizes,
  spacing,
};

export default theme;
